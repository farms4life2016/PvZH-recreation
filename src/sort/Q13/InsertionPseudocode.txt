SWAP = 0

loop N from 1 to ARRAY.length-1
	
	loop M from N to 1
		
		if ARRAY[M] < ARRAY[M-1] then
			
			SWAP = ARRAY[M]
			ARRAY[M] = ARRAY[M-1]
			ARRAY[M-1] = SWAP
		else
			break
		end if
		M = M - 1
	end loop
	N = N + 1
end loop